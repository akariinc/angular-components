Custom Sanitizer allows UI design system based on Angular CDK and Material, to easily set html element inside the components, without svg element to be removed by Angular's default sanitizer.


### Custom Sanitizer
Use Custom Sanitizer by importing the module and inject CustomSanitizer into where it should work.